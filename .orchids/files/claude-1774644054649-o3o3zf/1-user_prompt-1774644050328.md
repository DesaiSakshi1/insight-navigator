Add an interactive Column Deep Dive feature to 
my AutoEDA dashboard.

In the Dataset Overview tab, make every column 
name in the column summary table clickable. 
Each column name should:
- Have a blue underline on hover
- Show a cursor pointer
- Have a small "🔍" icon appearing on hover

When a column name is clicked, open a slide-in 
panel from the RIGHT side of the screen (like a 
drawer/sidebar):

Panel width: 420px
Panel background: #0F172A
Panel has an X close button top right
Panel title: "🔍 Deep Dive — [Column Name]"
Subtitle: data type badge (blue for numeric, 
purple for categorical)

Inside the panel show these sections:

SECTION 1 — Quick Stats (if numeric column):
4 small stat cards in 2x2 grid:
- Mean, Median, Std Dev, Range
Each card: dark background, colored value, 
label below

SECTION 2 — Distribution Chart:
- If numeric: render a small histogram using 
  Recharts AreaChart with cyan fill color
- If categorical: render a small horizontal 
  bar chart showing value counts
- Chart height: 180px
- Dark background, no border

SECTION 3 — Data Quality:
- "Missing Values: X (X%)" with colored badge
  (red >5%, orange 1-5%, green 0%)
- "Unique Values: X"
- "Most Frequent Value: X (appears Y times)"
- If numeric: "Outliers detected: X values"

SECTION 4 — Top Values (for categorical):
Show top 5 most frequent values as:
- Value name on left
- Count badge on right  
- Mini progress bar showing % of total
- Different pastel color per row

SECTION 5 — Recommendation box:
Light blue background card at bottom with 
lightbulb icon showing one smart recommendation 
for this column based on its stats.
Examples:
- "This column has high skewness — consider 
   log transformation"
- "Low cardinality column — suitable for 
   one-hot encoding in ML"
- "Clean column with no issues — ready for 
   feature selection"

Panel animation: slide in from right with 
smooth 300ms ease transition.
Clicking outside the panel or pressing Escape 
closes it with slide-out animation.
Make this work for ALL columns in the dataset.